You are an expert full-stack engineer working inside the existing Cyntric multi-tenant codebase. You must extend the current architecture without breaking any existing functionality, multi-tenancy isolation, security, or SOC II compliance. All changes must respect the shared PostgreSQL schema with tenant_id row-level filtering on every tenant-scoped table.
Core Architecture Reference (strictly adhere to this):

Shared schema, tenant_id FK everywhere with cascade deletes.
Users are global; tenant_members join table holds per-tenant roles and JSON capabilities.
Tenant secrets are already stored and managed per-tenant (used for Outlook/365). Expand this pattern.
Background jobs, email ingestion pipeline (agent@cyntric.com.au), document processing, Items registry (Parts), ChromaDB per-tenant, RAG/agent tools, and conversation/email forwarding all exist and must be reused/extended.
Global Admin manages tenant secrets and high-level config; tenant admins access via existing Integrations UI page.
No new UI screens or tenant-admin capability toggles for this feature.

Task: Implement Forge ERP Integration for Agentic Quoting
Business Goal (exact):
When a customer emails a parts list + machine details to the dedicated tenant inbox (agent@cyntric.com.au or configured address), the Cyntric agent will:

Parse the email using existing RAG + vision tools + knowledge base.
Identify requested items by matching against the tenant’s Items registry using the exact ERP item attribute value.
For items meeting confidence threshold, perform live Forge ERP lookups and create a new quotation.
Forward the original email to the tenant’s desired address (e.g. parts@trtaust.com.au) with:
Quote reference in subject.
Explanation, supporting images/extracts.
List of recognized items added.
List of omissions with reasons (no info, conflicting info, low confidence) and request for human action (clarify / research & contribute to KB).

Never manipulate the quote form in Forge. Only create new quotes. Edge cases and partial matches go only in the email body — never in the Forge quote.

Constraints (non-negotiable):

Developer/agent access only — no new tenant-facing UI beyond existing Integrations config.
One-way: Cyntric drives Forge like a human operator.
All operations tenant-scoped.
Use existing secrets store (expand assessment).
Exact match on Cyntric ERP item attribute for item lookup.
Only new quote creation at this stage.

Step-by-step Implementation Requirements:

Secrets & Configuration (extend existing)
In the tenant secrets / Integrations section (existing code), add support for Forge ERP config:
forge_erp_base_url
forge_erp_api_token (Bearer token)
forge_erp_service_account_email (optional, for audit)
quote_forward_to_email (tenant’s parts email)

Prompt the agent (in code comments and runtime) to first assess current tenant secrets infrastructure for Outlook integration and expand it cleanly for ERP (add new fields, encryption, UI form fields in Integrations page if missing).
Global Admin can set these per tenant.

Agent Tool / Background Job Extension
Extend the existing email ingestion → agent pipeline to detect quoting intents (use existing RAG + new tool).
Create a new agent tool create_forge_quotation that:
Takes parsed items (with quantities), customer name/reference, email context.
First resolves customer via GET {base_url}/master-data/customers?search={customer_name}.
For each item: confirm via GET {base_url}/master-data/items?search={erp_item_code} (must be exact match from Cyntric ERP item attribute).
Only proceed if all required lookups succeed with high confidence.
Build and POST to {base_url}/sales/quotations exactly as specified:



JSON{
  "customerId": "...",
  "customerName": "...",
  "currencyCode": "AUD",
  "lines": [ { "lineType": "stock", "itemId": "...", "itemCode": "...", "itemName": "...", "quantity": X, "unitOfMeasure": "...", "unitPrice": ..., "discountPct": 0, "taxPct": 10 } ],
  "notes": "Generated by Cyntric AI - Quote ref: QT-XXXXXX"
}

On success, capture returned id and auto-generated reference code.
Log the entire transaction (audit-compliant).


Error & Confidence Handling
Never invent IDs, prices, or codes — always resolve live.
If lookup fails or no match: do not include in quote. Record in email body with reason and call-to-action.
Low-confidence items → email only.
API errors, rate limits, network issues → graceful fallback to email notification + retry job if appropriate.

Email Forwarding
Extend existing forwarding logic.
Subject: Forge Quote QT-XXXXXX - [Original Subject]
Body: Rich summary with images from original email, table of added items, omissions table, quote link/reference, request for clarification/KB contribution.

Security & Compliance
All API calls use tenant-scoped secrets.
Full audit logging of every Forge call (who/what/when).
Respect account lockout, session rules, password policies (no impact).
Cascade delete will automatically clean up any new job records on tenant deletion.

Testing & Safety
Add comprehensive unit + integration tests (mock Forge API).
Include dry-run mode flag in config.
Ensure multi-tenant isolation in all new code paths (never leak across tenants).
Handle pagination/search nuances in customer/item lookups.


Implementation Approach (follow this order):

Extend tenant secrets model + Integrations UI backend/frontend (minimal changes).
Add new agent tool definition and implementation.
Hook into existing email ingestion / background job pipeline for quoting flow.
Update RAG/item matching to prioritize ERP item attribute.
Implement email summary/forwarding enhancements.
Add logging, error handling, tests.
Update any relevant documentation or admin views (Global Admin should see integration status).

Coding Standards:

TypeScript / existing stack.
Strict tenant_id filtering everywhere.
Reuse existing services for auth, secrets, jobs, email, RAG, vision.
Clean, well-commented, maintainable code with clear separation of concerns.
Zero breaking changes to existing features.

Begin by exploring the current codebase (secrets, integrations, background jobs, agent tools, email processor, Items model) and confirm how Outlook integration is implemented. Then implement the Forge ERP integration exactly as specified above. Provide a summary of files changed and any config/migrations needed when complete.